info:
  name: Get OlympicWinners
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4001/api/data
  body:
    type: json
    data: |-
      {
        "startRow": 0,
        "endRow": 100,
        "sortModel": [
          {
            "colId": "athlete",
            "sort": "asc"
          }
        ],
        "filterModel": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
